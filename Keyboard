#include<iostream>
#include<algorithm>
using namespace std;
int main() {
  string s="qwertyuiopasdfghjkl;zxcvbnm,./";
  char c; cin>>c;
  string a; cin>>a;
  if(c=='R')
{
  for(int i=0; i<a.size();i++)
cout<<s[s.find(a[i])-1];
}
  else
  for(int i=0; i<a.size();i++)
cout<<s[s.find(a[i])+1];
}
